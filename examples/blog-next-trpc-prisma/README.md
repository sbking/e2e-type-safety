# Blog Example - NextJS + tRPC + Prisma

This directory contains an example blogging application using NextJS, a tRPC API, and a PostgreSQL database with Prisma.

## Getting Started

```sh
cp .env.example .env
pnpm install
pnpm run docker
pnpm run setup
pnpm run dev
```
